export const metadata = {
  title: "API Gateway Service",
  description:
    "High-throughput gateway with JWT auth, rate limiting, Redis caching.",
};

# API Gateway Service

**High-Throughput Gateway** · 2024

High-throughput gateway with JWT auth, rate limiting, Redis caching. 150ms
response improvement. 500 concurrent requests sustained under load testing.

## Technical Stack

TypeScript, Node.js, Redis, PostgreSQL, Docker
